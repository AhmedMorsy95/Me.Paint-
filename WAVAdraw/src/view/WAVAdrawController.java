package view;

import javafx.fxml.FXML;
import javafx.scene.canvas.Canvas; //Unsure whether this should be here or elsewhere
import javafx.scene.control.Button;
import javafx.scene.control.ButtonBar;
import javafx.scene.control.Label;
import javafx.scene.control.SplitMenuButton;

// this class will serve as Scene Builder's controller
// the name implies it should be in the controller package, but apparently
// javafx.fxml.FXML import would only work if it's in the same package as FXMLs
// there will (probably) be another Controller in the Controller package
// and I will call it WAVAdrawControllerController
// LOL
// WAVAdrawController is where we start binding individual GUI elements with their functionalities
// I will leave this for tomorrow, soz

public class WAVAdrawController {
	
}
